# HTTP-FileHoster
A HTTP server that zips up your desired files and hosts it.

## Requirements
```
pip install PyQt5
```

## Usage
- Run main.py
- Put the files you want to host in the "Storage" folder
- Put in the host and port start server
